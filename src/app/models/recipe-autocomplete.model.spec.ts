import { RecipeAutocomplete } from './recipe-autocomplete.model';

describe('RecipeAutocomplete', () => {
  it('should create an instance', () => {
    expect(new RecipeAutocomplete()).toBeTruthy();
  });
});
